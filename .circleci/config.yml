version: 2
jobs:
  publish:
    docker:
      - image: laravelapp
    steps:
      - checkout
      - run:
          name: git config
          command: |
            git fetch
            git checkout .
workflows:
  version: 2
  build_site_and_publish:
    jobs:
      - publish:
          filters:
            branches:
              only: master
